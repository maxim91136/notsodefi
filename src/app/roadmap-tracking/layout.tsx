import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Roadmap Tracking - NotSoDeFi.com',
  description: 'Progressive decentralization roadmaps vs current status for crypto projects.',
};

export default function BrokenPromisesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
