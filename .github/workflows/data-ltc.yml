name: "LTC: Fetch Network Data"

on:
  schedule:
    - cron: '35 5 * * *'  # Daily at 5:35 UTC
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci

      - name: Fetch LTC data
        run: npx tsx scripts/fetch-ltc.ts

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git diff --quiet data/litecoin.json || (
            git add data/litecoin.json
            git commit -m "chore(data): LTC - update network metrics"
            git push
          )
