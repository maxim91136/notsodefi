name: "Tether: Fetch Supply Data"

on:
  schedule:
    # Run daily at 14:00 UTC
    - cron: '0 14 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Fetch Tether data
        run: npx tsx scripts/fetch-tether.ts

      - name: Commit changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add data/tether.json
          git diff --staged --quiet || git commit -m "chore(data): USDT - update supply metrics"
          git push
