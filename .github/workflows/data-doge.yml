name: "DOGE: Fetch Network Data"

on:
  schedule:
    - cron: '45 5 * * *'  # Daily at 5:45 UTC
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci

      - name: Fetch DOGE data
        run: npx tsx scripts/fetch-doge.ts

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git diff --quiet data/dogecoin.json || (
            git add data/dogecoin.json
            git commit -m "chore(data): DOGE - update network metrics"
            git push
          )
